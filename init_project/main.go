package main

import "fmt"

func main() {
    // var str string = "hello,世界"
    // fmt.Println(str)
    // fmt.Println(str[0])
    // for _, v := range str {
    //     fmt.Println(byte(v) == str[0])
    //     fmt.Println(v)
    // }

    // var bytes []byte = []byte(str)
    // for i:=0;i < len(bytes);i++ {
    //     fmt.Println(bytes[i])
    // }

    var r1 rune = 'a'
    var r2 rune = '世'
    fmt.Println(r1)
    fmt.Println(r2)
}